{
  "name": "@agentpager/relay",
  "version": "0.1.0",
  "description": "AgentPager cloud relay — Cloudflare Workers + Durable Objects",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute agentpager --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute agentpager --local --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentpager/protocol": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  },
  "author": "Piyush Vyas",
  "license": "Apache-2.0"
}
