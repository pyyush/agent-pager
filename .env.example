# Slack App Tokens (from api.slack.com/apps)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-level-token

# Slack channel ID where session threads are posted (used when SLACK_USER_ID is not set)
# Right-click channel in Slack -> "View channel details" -> scroll to bottom for ID
SLACK_CHANNEL_ID=C0123456789

# Your Slack user ID — enables DM-based notifications instead of channel
# Click your profile in Slack -> "..." -> "Copy member ID"
# SLACK_USER_ID=U0123456789

# Comma-separated Slack user IDs allowed to use /pager and reply to threads
# Empty = allow all (single-user setups)
# ALLOWED_SLACK_USERS=U0123456789

# Bridge HTTP port (hooks POST events here)
BRIDGE_PORT=7890

# Shared secret for authenticating hook requests to the bridge
# Generated automatically by setup.sh — do not wrap in quotes
# BRIDGE_SECRET=

# Default agent when none specified: claude or codex
PAGER_DEFAULT_AGENT=claude

# Log level: debug, info, warn, error
LOG_LEVEL=info
